from setuptools import setup, find_packages

setup(name='agenda_tools', 
        version='1.0', 
        description='Paquetes de funciones auxiliares para el scraper de agenda',
        author='Cesar',
        packages=['agenda_tools']
        )
